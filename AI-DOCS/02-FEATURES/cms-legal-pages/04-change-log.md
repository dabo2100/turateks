# Change Log — CMS & legal pages

### 2026-08-23 — Contact form + NAP + map
- `/iletisim` form emails via SMTP (`CONTACT_TO_EMAIL` or settings email), Sonner toast, honeypot.
- Address/phone/WhatsApp from settings. Map embed from NAP address.
- Admin page editor can set `seoTitle` / `seoDesc`.

### 2026-08-23 — Page model + public routes + admin editor
- `Page` table, default legal slugs, `/hakkimizda` `/kvkk` `/mesafeli-satis` `/iade` `/on-bilgilendirme` `/iletisim` render from DB.
- Admin editor: `/admin/sayfalar`.

### 2026-08-22 — Feature docs created
- **Author:** Planning session
