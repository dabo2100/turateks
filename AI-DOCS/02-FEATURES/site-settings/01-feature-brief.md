# Feature Brief — Site settings

## Summary

Single NAP + tax mode (included vs extra %) + “print terms on invoice” + WhatsApp number. Admin edits; storefront consumes.

## In scope

- `Setting` key/value or typed row
- Defaults for launch
- Footer/contact/schema read from here

## Out of scope

- Per-branch settings
- Full shipping matrix (can add keys later)
