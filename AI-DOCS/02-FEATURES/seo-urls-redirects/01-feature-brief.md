# Feature Brief — SEO, URLs, redirects

## Summary

TR metadata, sitemap, canonicals, JSON-LD, 301 from WordPress permalinks.

## In scope

- Metadata API
- `sitemap.ts` / `robots.ts`
- Redirect table (old `/urun/` → `/urunler/`, leftover `/shop`, `/hizmetlerimiz`)
- Noindex cart/account/admin

## Out of scope

- Mass tag archives
- English locale
